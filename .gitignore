# Node
node_modules/

# 파생 SQLite FTS5 인덱스(index.json+PNG 원본으로부터 재빌드 가능 — 절대 커밋 금지)
*.sqlite
*.sqlite-wal
*.sqlite-shm
library/index.sqlite*

# 스타일 앵커 원본(상용 게임 레퍼런스 가능 — 재배포 금지, 절대 커밋 금지. ADR-0001)
refs/*
!refs/README.md

# OS
.DS_Store
Thumbs.db

# 런타임 산출물(플러그인 자체에는 커밋 안 함 — 소비 프로젝트에서만 생성)
log/
generations.log

# oh-my-claudecode 세션/프로젝트 상태(로컬 런타임 — 커밋 금지)
.omc/

# PixelLab MCP 설정(project scope). API 키가 들어있고, git 에 올리면 공개 유출 + 플러그인 번들로
# 글로벌 활성화되므로 절대 커밋 금지 — pixellab-forge 작업 디렉터리에서만 로컬로 활성.
.mcp.json
